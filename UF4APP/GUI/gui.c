#include "gui.h"

/*
 * GUI implementation moved to gui_screen.c.
 * Keep this file as a lightweight translation unit so existing project
 * references to gui.c remain valid without exporting duplicate symbols.
 */
